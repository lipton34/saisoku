# モバイル中心の機能再編・改修仕様

最終更新: 2026-07-23

## 1. この文書の目的

この文書は、Saisokuの機能再編とモバイル向け改修について、議題、決定事項、未決事項、実装要件を一元管理するための正本である。

検討は議題を一つずつ進める。各議題について、質問と追加確認事項をすべて解決し、決定内容をこの文書へ反映してから次の議題へ移る。

最終的には、この文書だけで改修対象、期待動作、データ移行、権限、画面構成、検証内容を判断して実装を開始できる状態にする。

## 2. 検討の進め方

1. 「議題一覧」の順番で一つの議題だけを扱う。
2. 選択肢、影響範囲、推奨案を確認する。
3. 回答から発生した質問と追加確認事項を解決する。
4. 決定内容を「確定事項」と対象機能の仕様へ記録する。
5. 対象議題を `確定` にしてから次へ進む。
6. 全議題の確定後、要件間の矛盾と実装漏れを横断確認する。
7. 実装開始条件をすべて満たした後に改修作業へ移る。

検討中の内容を、確定した仕様として実装しない。

## 3. 前提

- 利用者は固定メンバー5人である。
- 主な閲覧・操作環境はスマートフォンなどの携帯端末である。
- 現在の画面はPC利用を前提とした情報量とレイアウトが多く、スマートフォンで見づらい。
- GBF本体で完結する機能は利用頻度が低い。
- SaisokuはGBF本体の代替ではなく、団内での共有、相談、記録、周回補助に価値を置く。
- 既存機能は即座に削除するだけでなく、簡略化、統合、保管庫への移動による活用も検討する。
- 既存の認証、所有者判定、ユーザーデータを維持する。
- 新しい画面構成は360px幅を基準にし、768pxおよび1280px以上でも利用可能にする。

## 4. 現時点の確定事項

### 4.1 機能の分類

主メニューに残す機能:

- ホーム
- 周回目標（現在の素材目標管理）
- 進捗目標
- 古戦場目標計算
- 編成共有
- 目標管理（現在の団内目標管理）

保管庫へ移す機能:

- イベント予定
- 公式NEWS
- 個人タスク管理

完全に削除する機能:

- ロードマップ
- 旧ツール・プレースホルダーは削除候補とし、正式な削除範囲を該当議題で確定する

利用者向けメニューへ表示しない内部基盤:

- 認証
- GBFデータマスタ
- 画像保存
- APIおよびDB基盤

### 4.2 周回目標

- 素材目標管理の利用者向け名称を「周回目標」へ変更する。
- 進捗目標とは統合しない。
- 素材数だけでなく、討伐回数や周回数などの数値目標にも使用する。
- 進捗目標より手軽に作成、確認、更新できる独立機能とする。

### 4.3 目標管理

- 現在の団内目標カンバンをホームの中心機能として表示する。
- 団内目標と個人目標をタブで分ける。
- 目標作成時に団内公開または個人用を選択できるようにする。
- 個人目標は完全非公開とし、作成者以外には存在自体を返さない。
- 画面上の非表示だけに頼らず、APIの取得、更新、削除で所有者と公開範囲を検証する。

### 4.4 編成共有

- キャラクター、武器、召喚石、ジョブなどを個別選択して編成を作成する手順を廃止する。
- 画像投稿を基本手順とする。
- 画像に収まらない情報や補足を入力するテキストフォームを用意する。
- 選択式の詳細入力は任意機能としても残さない。
- この方針は `docs/01_overall_policy.md` の現行方針と衝突するため、実装前に全体方針と編成関連要件を正式に更新する。

## 5. 議題一覧

状態は `未着手`、`検討中`、`確定` のいずれかで管理する。

| No. | 議題 | 状態 | 主な決定対象 |
| --- | --- | --- | --- |
| 1 | 共通ナビゲーション | 確定 | 主メニュー、順番、保管庫入口、スマホナビゲーション |
| 2 | 共通モバイルレイアウト | 確定 | 情報密度、主要操作、タブ、ダイアログ、共通表示規則 |
| 3 | 名称と用語 | 確定 | 目標管理、保管庫、編成投稿などの表記 |
| 4 | ホームの役割 | 確定 | 目標ボード以外の表示、ショートカット、各種要約 |
| 5 | 目標の公開範囲と権限 | 確定 | 公開範囲変更、編集、削除、状態変更、提案 |
| 6 | 目標カンバンと詳細画面 | 検討中 | モバイル表示、カード情報、操作、独立ルート |
| 7 | 周回目標 | 未着手 | 入力項目、増減操作、単位、プリセット、完了後の扱い |
| 8 | 進捗目標 | 未着手 | モバイル表示、ホーム要約、周回目標との使い分け |
| 9 | 編成の新規投稿 | 未着手 | 画像必須、画像数、補足、分類情報、URL |
| 10 | 編成の一覧・詳細・検索 | 未着手 | サムネイル、表示情報、画像閲覧、検索条件 |
| 11 | 既存編成データの移行 | 未着手 | 選択式データ、旧プリセット、コピー、編集互換性 |
| 12 | 古戦場目標計算 | 未着手 | 常設または期間限定表示、ホーム要約、過去計画 |
| 13 | 保管庫の共通仕様 | 未着手 | 入口、画面構成、利用可否、保守範囲 |
| 14 | イベント予定 | 未着手 | 保持機能、ホーム連携、NEWS連携 |
| 15 | 公式NEWS | 未着手 | 一般向けまたは管理向け、取得処理、ログ |
| 16 | 個人タスク | 未着手 | 保持機能、ホーム集計、個人目標との役割分担 |
| 17 | ロードマップと旧ツールの削除 | 未着手 | ルート、リンク、コンポーネント、CSS、文書への退避 |
| 18 | データ移行と内部名称 | 未着手 | Prisma、API、URL、既存行、段階的廃止 |
| 19 | ドキュメント整合 | 未着手 | 全体方針、各機能要件、README、HANDOFF |
| 20 | 横断確認と実装計画 | 未着手 | 依存順、作業単位、検証、ロールバック |

## 6. 議題ごとの記録形式

各議題は、検討開始時に以下の形式でこの文書へ追記する。

```text
### 議題N: 議題名

状態:

目的:

現状:

確定事項:

未決事項:

質問・追加確認:

影響する画面:

影響するAPI:

影響するデータ:

移行・互換性:

スマートフォン要件:

完了条件:
```

## 7. 議題の検討記録

### 議題1: 共通ナビゲーション

状態: 確定

目的:

- スマートフォンを主対象として、普段使う機能へ少ない操作で移動できる構成にする。
- 主ナビゲーションへ表示する情報を減らし、低頻度機能を保管庫へ整理する。

現状:

- PC向けの左サイドバーに、ホーム、タスク、素材、進捗、目標、古戦場、NEWS、イベント、編成一覧、編成投稿、ロードマップが並んでいる。
- スマートフォンでもサイドバーを変形したナビゲーションを使用している。
- ホームとログアウトはサイドバー内にある。

確定事項:

- すべての画面幅で同じ下部ナビゲーションを使用する。
- 下部ナビゲーションの並び順は次のとおりとする。
  1. 周回目標
  2. 進捗目標
  3. 編成
  4. 古戦場
  5. 保管庫
- 下部ナビゲーションの各項目には、用途を判別できる専用アイコンを表示する。
- 既存のアイコンに用途の合うものがあれば流用し、不足する場合だけ新しいアイコンを選定する。
- 「その他」は設けない。
- 保管庫は専用画面とする。
- 古戦場は開催時期に関係なく常時アクセス可能にする。
- 目標管理は独立した主メニュー項目として残さず、ホームへ集約する。
- ホームボタン、ユーザー情報、ログアウトボタンはヘッダーへ移す。
- ヘッダーは下方向へのスクロールで隠し、上方向へのスクロールで再表示する。ページ最上部では常に表示する。
- ヘッダーの左側にホームアイコンと「Saisoku」を表示する。
- ヘッダーの右側にユーザー名とログアウトアイコンを表示する。
- スマートフォンで幅が不足する場合はユーザー名を省略し、アイコンだけを表示する。

未決事項:

- なし。

質問・追加確認:

- すべて解消済み。

影響する画面:

- 認証後の全画面
- ホーム
- 保管庫の新規画面

影響するAPI:

- ナビゲーション自体によるAPI変更はない。
- ログアウトは既存の認証APIを継続利用する。

影響するデータ:

- なし。

移行・互換性:

- 既存ルートの変更有無は、各機能およびデータ移行の議題で決める。
- 保存済みURLから各画面へ直接アクセスできる構成を維持する。

スマートフォン要件:

- 360px幅で5項目の下部ナビゲーションが収まり、ラベルとアイコンを判別できること。
- 下部ナビゲーションが本文の操作や最終行を隠さないこと。
- ヘッダーが非表示でも、主要5項目間を移動できること。
- ログアウトは誤操作しにくく、再表示可能な場所へ置くこと。

完了条件:

- 達成済み。

### 議題2: 共通モバイルレイアウト

状態: 確定

目的:

- 360px幅を基準に、情報量が多すぎず、主要な閲覧と操作を迷わず行える共通レイアウトを定める。
- 画面ごとに異なる操作方法を増やさず、タブ、カード、作成操作、ダイアログの扱いを統一する。
- 768pxおよび1280px以上でも、スマートフォンと同じ機能と操作順を維持しながら余白を活用する。

現状:

- PC向けに複数列、横長のカンバン、多数のボタンや説明を同時表示する画面がある。
- スマートフォン向けの調整はあるが、画面ごとに情報密度と操作方法が異なる。
- 作成・編集にダイアログとページ内フォームが混在している。

確定事項:

- スマートフォンなどの携帯端末を主対象とする。
- 最小対応幅は320pxとし、360px、768px、1280px以上で確認する。
- すべての画面幅で同じ下部ナビゲーションを使用する。
- 各画面の主要な新規作成ボタンは、下部ナビゲーションと重ならない画面右下へ固定表示する。
- 作成機能がない画面では、固定の新規作成ボタンを表示しない。
- タブは画面上部へ横並びで配置し、横幅に収まらない場合はタブ領域内だけ横スクロール可能にする。
- 入力項目が少ない作成・編集はダイアログを使用し、入力項目が多い場合は専用画面へ移動する。
- ダイアログと専用画面を分ける目安は入力3項目程度とし、入力内容や操作の複雑さに応じて個別機能の議題で最終判断する。
- PC・タブレットでも基本的な画面構造は変えず、本文に最大幅を設定し、余裕がある一覧だけ複数列にする。
- PC・タブレットでの具体的な最大幅と列数は、各画面の改修後に実表示を確認して決める。
- 説明、完了済み情報、補助操作などの優先度が低い情報は初期表示を簡潔にし、利用者の展開操作によって表示する。
- 一覧画面は、判断に必要な要点だけをカードへ表示し、カードをタップすると詳細画面へ移動する形式を共通の基本方針とする。
- 一覧内で素早く更新する価値がある操作は、個別機能の議題で例外として定める。

未決事項:

- なし。

質問・追加確認:

- すべて解消済み。

影響する画面:

- 認証後の全画面
- 共通ヘッダー
- 共通下部ナビゲーション
- 一覧、カード、タブ、フォーム、ダイアログを使用する各画面

影響するAPI:

- なし。個別機能の表示件数やページングが必要な場合は、該当議題で決める。

影響するデータ:

- なし。

移行・互換性:

- 既存データは変更しない。
- 既存画面の操作導線は変わる可能性があるが、各機能の実データへ引き続きアクセスできるようにする。

スマートフォン要件:

- 主要な閲覧と操作を縦方向の流れで完結できること。
- ページ全体に意図しない横スクロールを発生させないこと。
- 下部ナビゲーション、固定操作、入力中のキーボードがコンテンツを隠さないこと。
- 主要なタップ対象は可能な範囲で約44px以上を確保すること。
- 長い日本語、URL、ユーザー入力がカードや画面幅を押し広げないこと。

完了条件:

- 達成済み。

### 議題3: 名称と用語

状態: 確定

目的:

- 短い表示領域でも意味が伝わる名称へ統一する。
- 同じ機能を画面ごとに異なる名称で呼ばないようにする。
- 団内公開と個人用の両方を扱う機能から、団内専用に見える表現を取り除く。

現状:

- 「素材メモ」「素材目標管理」など、周回数や討伐数にも使用できる実態より用途が狭く見える名称がある。
- 「団内目標」「目標共有」など、今後追加する個人目標を含まない名称がある。
- 編成機能では「編成一覧・検索」「編成投稿」「編成共有」など複数の表現を使用している。
- 「必要数の進捗」と「進捗目標」の表記が混在している。

確定事項:

- 現在の素材目標管理の利用者向け名称は「周回目標」とする。
- 低頻度機能をまとめる専用画面の名称は「保管庫」とする。
- 下部ナビゲーションでは短い名称を使用する。

未決事項:

- なし。

質問・追加確認:

以下の利用者向け名称で統一する。

| 対象 | 確定名称 |
| --- | --- |
| ホームに集約する目標機能全体 | 目標 |
| 公開範囲のタブ・選択肢 | 団内目標 / 個人目標 |
| 現在の素材目標管理 | 周回目標 |
| 段階式の進捗管理 | 進捗管理 |
| 編成機能全体・下部ナビゲーション | 編成 |
| 編成を新しく追加する操作 | 投稿 |
| 既存編成を変更する操作 | 編集 |
| 古戦場目標計算・下部ナビゲーション | 古戦場 |
| 低頻度機能の専用画面 | 保管庫 |
| 新しい目標や周回目標を追加する操作 | 作成 |
| 入力内容を確定するボタン | 保存 |

- すべて解消済み。

影響する画面:

- 認証後の全画面
- ヘッダー、下部ナビゲーション、ページ見出し、タブ、ボタン、空状態

影響するAPI:

- 利用者向け名称だけの変更では影響しない。
- API、URL、Prismaなどの内部名称は議題18で決める。

影響するデータ:

- なし。

移行・互換性:

- なし。内部名称の変更は議題18で扱う。

スマートフォン要件:

- 下部ナビゲーションでは、360px幅でアイコンと名称を判別できる短さにする。
- ボタンは動作が分かる動詞を使用し、同じ操作に複数の表現を使わない。

完了条件:

- 達成済み。

### 議題4: ホームの役割

状態: 確定

目的:

- ホームを目標確認と目標操作の中心画面にする。
- 下部ナビゲーションと重複する機能一覧や、保管庫へ移す個人タスクの情報を整理する。
- アプリを開いた直後に、団内目標または個人目標を確認できるようにする。

現状:

- 今日やることを示す大きな案内領域がある。
- 優先タスク5件と団内目標5件を表示している。
- 実装済み機能への大きなカードリンク一覧を表示している。
- 目標の全カンバンは独立した `/goals` 画面にある。

確定事項:

- 現在の目標カンバンをホームの中心機能として表示する。
- 団内目標と個人目標をタブで分ける。
- 目標管理を独立した主メニュー項目として残さない。
- 主要機能への移動は共通の下部ナビゲーションで行う。
- ホームの基本構成は「目標」の見出し、団内目標・個人目標タブ、目標カンバン、右下の目標作成ボタンとする。
- 現在の大きな案内領域、優先タスク、機能カード一覧はホームから削除する。
- 初期表示するタブは「団内目標」とする。
- 編成、周回目標、進捗管理、古戦場の要約はホームへ追加しない。
- ホーム上で目標の閲覧、タブ切り替え、新規作成、状態変更を行えるようにする。
- 詳しい編集と関連情報の管理は、目標カードから目標詳細へ移動して行う。
- 周回目標および進捗管理の項目を、ホームの目標ボードと連携できるようにする。
- 連携した項目のボード上の状態は、目標ボード側で変更できるようにする。
- 新規目標の公開範囲は個人目標を初期値とする。
- 個人目標は、確認メッセージを表示したうえで団内目標へ変更できる。
- 団内目標から個人目標への変更は禁止する。
- 周回目標・進捗管理の作成時に「目標ボードに表示」を初期ONで表示し、利用者が解除できるようにする。
- 周回目標・進捗管理から作成する連携カードは、個人目標として追加する。
- 連携カードへ値を複製せず、元の周回目標・進捗管理を正本として参照する。
- 元の周回目標・進捗管理を削除した場合、連携カードも削除する。
- 周回目標の数値は周回目標画面と目標カードの両方から変更でき、同じ値へ反映する。
- 周回目標のカード本体を押すと周回目標画面へ移動する。
- 進捗管理の内容変更は目標カード内で行わず、カード本体から進捗管理画面へ移動して行う。
- 既存の「今やる」「次にやる」「後でやる」「保留」「完了」とは別に、初期状態用の「未設定」を先頭へ追加する。
- 通常作成した目標と連携カードを含む、すべての新規目標を「未設定」から開始する。
- 状態変更はスワイプ操作を候補とし、画面サイズや操作環境に応じてドロップダウンなどの明示的な操作も提供する。具体的な操作は議題6で確定する。
- ホームを開いたときは、毎回「個人目標」タブを初期表示する。
- 個人目標から団内目標へ変更した周回目標の連携カードは、団内メンバーも数値を閲覧できるが、数値を変更できるのは作成者だけとする。
- 個人目標から団内目標へ変更した進捗管理の連携カードは、団内メンバーは参照だけ可能とし、変更は所有者が進捗管理画面で行う。

未決事項:

- なし。

質問・追加確認:

- すべて解消済み。

影響する画面:

- ホーム
- 現在の目標一覧
- 現在の目標詳細

影響するAPI:

- 団内目標と個人目標を取得するAPIが必要になる。取得条件と権限は議題5で決める。
- 現在ホームで取得している個人タスクAPIは不要になる可能性がある。
- 周回目標・進捗管理と目標ボードの連携作成、取得、解除を扱うAPIが必要になる可能性がある。

影響するデータ:

- 目標の公開範囲を保存する変更が必要になる。
- 周回目標・進捗管理と目標ボードの参照関係を保存する変更が必要になる可能性がある。
- 初期状態用のボード状態を追加する。

移行・互換性:

- 個人タスクのデータは削除せず、保管庫から引き続き利用できる。
- 現在の目標データは新しいホームで表示する。

スマートフォン要件:

- ヘッダー直下で目標タブと目標内容を確認できること。
- 右下の目標作成ボタンが下部ナビゲーションやカード操作を隠さないこと。
- カンバンのモバイル表示方法は議題6で決める。

完了条件:

- 達成済み。

### 議題5: 目標の公開範囲と権限

状態: 確定

目的:

- 個人目標を他ユーザーへ漏らさず、団内目標では必要な共同利用ができる権限を定める。
- 画面上の表示制御だけでなく、APIの取得、更新、削除、関連情報操作へ同じ規則を適用する。
- 個人目標から団内目標への不可逆な公開を、安全に実行できるようにする。

現状:

- 既存の目標は認証済みユーザー全員が一覧で閲覧できる。
- 既存の目標本体、ボード状態、関連情報を変更できるのは所有者だけである。
- 目標を他ユーザーへ提案し、受け入れまたは見送りできる機能がある。
- 個人目標と公開範囲の概念はまだない。

確定事項:

- 新規目標は個人目標として作成する。
- 個人目標は所有者以外に存在自体を返さない。
- 個人目標から団内目標への変更は可能とする。
- 団内目標から個人目標へ戻すことは禁止する。
- 団内目標への変更時は、全員へ公開され元に戻せないことを確認するポップアップを表示する。
- 周回目標と進捗管理の連携カードは個人目標として作成し、団内目標へ変更できる。
- 団内公開した連携カードの参照元データを変更できるのは所有者だけとする。
- 団内目標のタイトル、説明、期限などの内容を編集できるのは作成者だけとする。
- 団内目標のカンバン状態を変更できるのは作成者だけとする。
- 団内目標を削除できるのは作成者だけとし、削除前に確認を表示する。
- 関連編成、必要武器、討伐目標、サブタスクを追加、編集、削除できるのは作成者だけとする。
- 他ユーザーへの目標提案、提案の受け入れ、見送り機能は削除する。
- 個人目標を団内公開する確認画面には、「団内の全員に公開され、個人目標には戻せません」と表示する。
- 団内公開は、通常の保存と区別した「団内目標として公開」ボタンで確定する。

未決事項:

- なし。

質問・追加確認:

- すべて解消済み。

影響する画面:

- ホーム
- 目標作成
- 目標詳細
- 目標提案

影響するAPI:

- 目標一覧と詳細取得
- 目標作成、更新、削除
- ボード状態変更
- 関連編成、必要武器、討伐目標、サブタスク
- 目標提案

影響するデータ:

- 目標に公開範囲を保存する永続項目が必要になる。
- 既存目標の公開範囲を移行する必要がある。

移行・互換性:

- 既存目標は現在全員へ表示されているため、移行時は団内目標として扱う案を基本とする。正式決定は議題18で行う。
- 個人目標へ関連する情報も、所有者以外のAPIレスポンスへ含めない。

スマートフォン要件:

- 公開範囲を短いラベルだけでなく、意味の分かる説明とともに選択できること。
- 不可逆な団内公開と削除は誤操作で確定しないこと。
- 権限がない操作は非表示または無効にし、APIでも拒否すること。

完了条件:

- 達成済み。

### 議題6: 目標カンバンと詳細画面

状態: 検討中

目的:

- 6つの状態をスマートフォンで見やすく表示し、作成者が少ない操作で状態を変更できるようにする。
- カードの情報量を抑えながら、詳細確認と連携元機能への移動を分かりやすくする。
- スワイプだけに依存せず、タッチ、キーボード、PCでも同じ操作結果を得られるようにする。

現状:

- 「今やる」「次にやる」「後でやる」「保留」「完了」の5列を横方向へ並べている。
- カードにはカテゴリ、優先度、負荷、初心者向け、期限、進捗、必要武器数などを表示している。
- 状態変更はカード内のセレクトで行う。
- 通常目標の詳細では、編集、関連編成、必要武器、討伐目標、サブタスクなどを扱う。

確定事項:

- 「未設定」「今やる」「次にやる」「後でやる」「保留」「完了」の6状態を使用する。
- すべての新規目標は「未設定」から開始する。
- 状態を変更できるのは作成者だけとする。
- 状態変更にはスワイプを候補とし、ドロップダウンなどの明示的な操作も提供する。
- 通常目標のカードからは目標詳細へ移動する。
- 周回目標の連携カード本体からは周回目標画面へ、進捗管理の連携カード本体からは進捗管理画面へ移動する。

未決事項:

- 6状態の表示形式と初期表示する状態。
- カードに常時表示する情報。
- 状態変更ジェスチャー、明示操作、変更取り消しの方法。
- 絞り込み機能の要否と表示方法。
- 通常目標の詳細画面に残す情報と、スマートフォンでの表示形式。

質問・追加確認:

1. スマートフォンでは6状態を上部タブにし、選択中の状態だけを1列表示する形式でよいか。PC・タブレットの複数列化は実画面の改修後に検討する。
2. ホームを開いた直後の状態タブは「未設定」と「今やる」のどちらにするか。目標作成直後は自動的に「未設定」へ切り替える案を推奨する。
3. カードへ常時表示する情報を「タイトル、目標の種類、進捗または現在値、期限、作成者（団内目標のみ）」に絞ってよいか。優先度、負荷、初心者向けなどは詳細へ移すか、機能自体を後続議題で整理する。
4. スマートフォンではカードを左右へスワイプすると隣の状態へ移動し、変更後に短時間の「元に戻す」を表示してよいか。状態を飛ばして変更するため、作成者には常に状態選択メニューも表示する。
5. 団内目標の絞り込みは、初期状態では隠した「絞り込み」ボタンから作成者、種類、期限を選べるようにするか。個人目標では絞り込みを表示しない案でよいか。
6. 通常目標の詳細画面は残し、「概要」「関連情報」「サブタスク」を縦に並べて折りたたみ表示してよいか。編集と削除は作成者だけに表示する。

影響する画面:

- ホーム
- 目標詳細
- 目標作成・編集

影響するAPI:

- ボード状態変更
- 目標一覧・詳細取得
- 周回目標の数値更新

影響するデータ:

- ボード状態へ「未設定」を追加する。
- 既存状態と並び順を維持する。

移行・互換性:

- 既存目標の状態は変更しない。
- 新規目標だけを「未設定」で作成する。
- 既存の保存済みURLから目標詳細を開けるようにする。

スマートフォン要件:

- 6状態をページ全体の横スクロールなしで切り替えられること。
- スワイプ以外にも、見えている状態選択操作を用意すること。
- カード内の操作がカード本体の画面遷移を誤って発火しないこと。
- 状態変更後に結果が分かり、誤操作を取り消せること。

完了条件:

- 状態の表示、初期状態、カード情報、状態変更、絞り込み、詳細画面の構成が確定している。

## 8. 実装開始条件

以下をすべて満たすまで、機能再編の実装を開始しない。

- 議題1から20がすべて `確定` になっている。
- 各画面の目的、主要表示、主要操作が決まっている。
- 団内公開と個人用のAPI権限が決まっている。
- 既存目標と既存編成の移行方法が決まっている。
- 削除するルート、コンポーネント、CSS、API、データの範囲が決まっている。
- 360px、768px、1280px以上での期待動作が決まっている。
- `docs/01_overall_policy.md` と新しい編成方針の衝突を解消している。
- README、HANDOFF、関連機能要件の更新対象が決まっている。
- 実装を安全な作業単位へ分割し、各単位の検証方法が決まっている。
- 認証、所有者判定、既存データを壊さないことを確認できる。

## 9. 実装時の必須検証

- `npm run typecheck`
- `npm run build`
- 360px、768px、1280px以上での変更画面確認
- 団内目標を全利用者が取得できること
- 個人目標を所有者だけが取得、更新、削除できること
- 他ユーザーの個人目標が一覧、詳細、関連APIから漏れないこと
- 既存目標と既存編成を移行後も参照できること
- 保管庫へ移した機能へ意図した経路でアクセスできること
- 削除した画面へのリンクが残っていないこと
- ページ全体に意図しない横スクロールが発生しないこと

## 10. 変更履歴

- 2026-07-23: 初版作成。前提、確定事項、議題一覧、実装開始条件を記録。
- 2026-07-23: 議題1「共通ナビゲーション」の回答を反映し、追加確認事項を記録。
- 2026-07-23: 「その他」を廃止し、古戦場と保管庫を独立した下部ナビゲーション項目に変更。議題1を確定。
- 2026-07-23: 下部ナビゲーションの各項目へ専用アイコンを表示し、適切な既存アイコンを優先的に流用する方針を追加。
- 2026-07-23: 議題2「共通モバイルレイアウト」の検討を開始。
- 2026-07-23: 議題2の主要作成ボタン、タブ、入力画面、画面幅、低優先情報に関する方針を反映。一覧表示の共通方針のみ確認継続。
- 2026-07-23: 一覧表示の共通方針を確定し、議題2を完了。議題3「名称と用語」の検討を開始。
- 2026-07-23: 表示名称を確定して議題3を完了。議題4「ホームの役割」の検討を開始。
- 2026-07-23: ホームの基本構成を確定。周回目標・進捗管理と目標ボードの連携要望を追加し、連携仕様の確認を開始。
- 2026-07-23: 新規目標の初期公開範囲、公開範囲変更、連携作成、参照方式、削除連動、カード操作、「未設定」状態の方針を反映。
- 2026-07-23: ホームの初期タブと団内公開した連携カードの変更権限を確定し、議題4を完了。議題5「目標の公開範囲と権限」の検討を開始。
- 2026-07-23: 目標本体、状態、削除、関連情報の権限と提案機能の削除を確定し、議題5を完了。議題6「目標カンバンと詳細画面」の検討を開始。
